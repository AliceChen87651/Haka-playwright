{
  "name": "haka-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "E2E tests for 12basic.hakka.gov.tw using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:chromium": "playwright test --project=chromium",
    "show-report": "playwright show-report",
    "codegen": "playwright codegen https://12basic.hakka.gov.tw/"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
